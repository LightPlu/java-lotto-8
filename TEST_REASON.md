# 테스트를 왜 작성하는가

테스트를 왜 작성하는지에 대해 의문점이 생기지 않았었다.  
옛날에는 그저 단순한 프로그램을 만들었기 때문에, 아무리 프로젝트의 규모가 커지더라도 `System.out.println()` 메서드를 통해서 확인이 가능했다. 프로그램이 엄청 단순했기 때문이다.

하지만 테스트 코드를 작성하기 시작하면서 한 번 생각해보니, 과연 매번 메서드의 기능이 잘 돌아가는지 확인하기 위해서 항상 서버를 껐다 켜야 하나? 라는 생각이 들었다.  
만약 서버가 돌아가고 있는 상태에서 새로운 기능을 구현해야 하는데, 서버가 꺼지면 안 되는 상황에서는 테스트를 어떻게 하지? 라는 의문이 이번 프리코스를 진행하면서 생겼다.

테스트 코드 작성이란 것은 피드백에 적혀 있는 것처럼 **기능의 정확성을 점검함을 넘어 코드의 즉각적인 피드백을 받을 수 있다는 의미**가 중요하다고 생각이 든다.  
내 의도대로 코드를 만들고 싶어 코드를 구현했을 때, 과연 이것이 정말 내가 원하는 값을 리턴하는가?  
이에 대한 피드백을 바로바로 확인할 수 있게 만드는 것이 바로 테스트 코드이다.

상수를 처음 사용해보며 메시지를 출력하고, 해당 메시지에 입력에 따라 매개변수를 통해 변하는 숫자값을 포매팅하고 싶어했을 때, 테스트 코드를 작성하여 확인한 결과 내가 원하는 대로 출력이 나오지 않고 상수명 그대로 출력이 나와 테스트 코드가 실패했다.  
이처럼 내가 코드를 구현할 때 **의도한 대로 코드가 동작하는지 확인하기 위해선 테스트를 작성해야 한다**는 것을 뼈저리게 느꼈다.

물론 프로젝트가 작은 경우에는 `System.out.println()`을 사용하는 것이 편할 수 있다.  
하지만 서버 개발자를 준비하는 사람이 나에게 똑같이 물어본다면 이렇게 대답해주고 싶다.

> “단위 테스트를 작성해서 해당 메서드가 잘 돌아가는지 확인하는 게 좋을까,  
> 아니면 서버를 껐다 켰다 하면서 시스템이 멈추는 게 좋을까?”

이 질문 자체가 답이 된다고 생각한다.
